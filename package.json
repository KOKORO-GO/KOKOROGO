{
  "name": "kokorogo-site",
  "version": "1.0.0",
  "private": true,
  "description": "KokoroGo — AI-powered passion discovery & venture platform for girls aged 12-16",
  "scripts": {
    "dev": "npx serve public -l 3000",
    "build": "echo 'Static site — no build step required'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "resend": "^6.9.3"
  }
}
